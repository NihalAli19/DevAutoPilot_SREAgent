// Models — versions, eval metrics, drift charts, retrain button (MLOps proof).
// TODO(plan: Phase 1/5) — fetch model_runs and render drift charts.
export default function ModelsPage() {
  return (
    <main>
      <h1>ML / Models</h1>
      <p>Phase 0 placeholder.</p>
    </main>
  );
}
