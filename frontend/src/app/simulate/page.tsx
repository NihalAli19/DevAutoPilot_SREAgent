// Simulate — trigger anomalies/incidents on demand for live demos.
// TODO(plan: Phase 2) — POST to /api/simulate.
export default function SimulatePage() {
  return (
    <main>
      <h1>Simulate</h1>
      <p>Phase 0 placeholder.</p>
    </main>
  );
}
