// Postmortems — searchable archive.
// TODO(plan: Phase 3) — fetch and search stored postmortems.
export default function PostmortemsPage() {
  return (
    <main>
      <h1>Postmortems</h1>
      <p>Phase 0 placeholder.</p>
    </main>
  );
}
