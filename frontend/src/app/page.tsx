// Dashboard — live incident feed, stats, agent activity, model-health widget.
// TODO(plan: Phase 2) — wire to the backend /api/dashboard endpoint.
export default function DashboardPage() {
  return (
    <main>
      <h1>DevAutoPilot v2 — Dashboard</h1>
      <p>Phase 0 placeholder. Live incident feed and model-health widgets coming in Phase 2.</p>
    </main>
  );
}
