// Agents — status of all five agents, workload, token/cost per run.
// TODO(plan: Phase 3) — fetch agent status and traces.
export default function AgentsPage() {
  return (
    <main>
      <h1>Agents</h1>
      <p>Phase 0 placeholder.</p>
    </main>
  );
}
