// Incidents — list + detail with agent timeline and trace view.
// TODO(plan: Phase 2/3) — fetch incidents and render the agent timeline.
export default function IncidentsPage() {
  return (
    <main>
      <h1>Incidents</h1>
      <p>Phase 0 placeholder.</p>
    </main>
  );
}
