// ModelHealthCard — model version, eval metrics, and drift status widget.
// TODO(plan: Phase 1/5) — surface model_runs metrics and drift scores.
export default function ModelHealthCard() {
  return <div>{/* Phase 0 placeholder */}</div>;
}
