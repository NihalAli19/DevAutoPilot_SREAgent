// IncidentCard — summary card for a single incident.
// TODO(plan: Phase 2) — render severity, service, status, and confidence.
export default function IncidentCard() {
  return <div>{/* Phase 0 placeholder */}</div>;
}
