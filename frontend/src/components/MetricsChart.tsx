// MetricsChart — telemetry time-series chart (Recharts) with anomaly overlay.
// TODO(plan: Phase 2) — plot metric series and highlight anomalies.
export default function MetricsChart() {
  return <div>{/* Phase 0 placeholder */}</div>;
}
