// AgentTimeline — ordered timeline of agent steps for an incident.
// TODO(plan: Phase 3) — render agent_actions with tokens/latency/trace.
export default function AgentTimeline() {
  return <div>{/* Phase 0 placeholder */}</div>;
}
