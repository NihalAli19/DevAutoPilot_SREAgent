// Sidebar — primary navigation for the SRE control center.
// TODO(plan: Phase 4/5) — links to Dashboard, Incidents, Agents, Models, Postmortems, Simulate.
export default function Sidebar() {
  return <nav aria-label="Primary">{/* Phase 0 placeholder */}</nav>;
}
