"""Patch Agent — generates a minimal diff and opens a draft PR (never auto-merges)."""
# TODO(plan: Phase 3) — fetch file content, synthesize diff, validate, open draft PR.


class PatchAgent:
    """Proposes fixes as reviewable draft pull requests."""
