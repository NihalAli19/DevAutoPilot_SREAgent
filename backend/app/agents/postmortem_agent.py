"""Postmortem Agent — compiles the timeline, writes the report, re-embeds it."""
# TODO(plan: Phase 3) — generate postmortem and index it back into Azure AI Search.


class PostmortemAgent:
    """Closes the learning loop so future RCAs get smarter."""
