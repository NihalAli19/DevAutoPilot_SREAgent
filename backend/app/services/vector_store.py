"""Vector store — Azure AI Search (vector + hybrid) for incident/postmortem RAG."""
# TODO(plan: Phase 3) — index and query the incident knowledge base.


class VectorStore:
    """RAG retrieval over past incidents and postmortems."""
