"""Dataset loader — fetch and parse the NAB benchmark time series."""
# TODO(plan: Phase 1) — load NAB CSVs with labels into pandas frames.


def load_nab():
    """Return (frames, labels) from the Numenta Anomaly Benchmark."""
    raise NotImplementedError("TODO(plan: Phase 1)")
