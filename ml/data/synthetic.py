"""Synthetic generator — inject anomalies into a metrics stream for live demos."""
# TODO(plan: Phase 1) — generate seasonal series with injected spikes/dips.


def generate(n_points: int = 1000):
    """Return a synthetic metric series with labeled anomalies."""
    raise NotImplementedError("TODO(plan: Phase 1)")
