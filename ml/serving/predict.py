"""Serving — load the registered model and score telemetry points."""
# TODO(plan: Phase 1) — load from MLflow registry and expose a score() function.


def score(features):
    """Return an anomaly score (and flag) for an engineered feature row."""
    raise NotImplementedError("TODO(plan: Phase 1)")
